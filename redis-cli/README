A remote Redis.io based object store used as a cache.

Define and implement an interface on Redis object serialization using  
"encoding/gob" standard package.

https://go.dev/blog/gob

In order to link to this module, use the import:

  import (
     "github.com/mrgusvali/rediscli"
  )

Then declare:
  
  $ go mod edit -replace github.com/mrgusvali/rediscli=../redis-cli
  $ go mod tidy
